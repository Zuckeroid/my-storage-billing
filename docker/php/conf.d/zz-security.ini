; Disable shell/process execution functions that FOSSBilling flags as dangerous.
; The application does not rely on them in our deployment flow.
disable_functions=exec,passthru,system,shell_exec,popen,proc_open,pcntl_exec
